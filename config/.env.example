# StreamServer 环境变量示例
# 复制为 .env 并按需修改；.env 不入库（见 .gitignore），仅本示例文件入库。

# 运行环境
NODE_ENV=development

# 端口
HTTP_PORT=8000
RTMP_PORT=1935

# RTMP 路由
RTMP_APP=live

# HLS
HLS_FRAGMENT_SEC=6
HLS_WINDOW_SIZE=5

# 鉴权（URL 签名密钥；生产环境必须更换并从密钥管理系统获取）
AUTH_SECRET=change-me-in-production

# 日志
LOG_LEVEL=info
