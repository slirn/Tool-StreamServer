# M1 · 工程骨架 · Plan

- 状态：**已批准并完成**（2026-08-27 合入 main，commit dc6a2e2 / 0e16905）
- 分支：feature/m1-skeleton（已合并删除）

## 1. 背景与目标

- 背景：规范与配置就绪，但 npm install 未执行、src/ 不存在、CI/钩子未接线。
- 目标：搭建可编译、可测试、有提交防护的最小工程骨架（不含媒体业务逻辑）。
- 验收标准：`npm run lint / test / build` 全绿；`git commit` 被 commitlint 钩子拦截非法 message；核心模块接口骨架就位且依赖方向经测试验证。

## 2. 方案要点

- 分层骨架：src/ 按 ARCHITECTURE 建目录，每层接口文件 + 类型定义；核心抽象先行（StreamRegistry / StreamEvent）。
- 依赖方向守护：Vitest 架构测试做静态 import 分析（core 不 import 上层、ingress↔egress 互不引用、堵动态 import/require）。
- 配置：env + 默认值 + 手写校验（不引 zod），非法 fail-fast。
- 测试：config loader 单测 + 架构守护测试。

## 3. 实际结果（含 review 修复）

- 19 文件 +4596 行；lint / 15 测试 / build 全绿；钩子负向测试三连通过。
- 子代理审查 14 项发现：日志嵌套脱敏缺失、NODE_ENV 缺失生产 fail-open、序列化崩溃路径、lint 规则与规范矛盾等，全部修复并补测试兜底。
- DoD 报告见会话记录；ADR 无新增（M1 未触碰架构决策）。

## 4. 遗留

- 远程仓库名 Tool-StramServer 待改（GitHub 不可达，挂起）。
- M2 前需决策 NMS 集成方式（已在 M2 Plan 完成）。
